// import { useScript } from "deco";

// export const useDeviceType = () => {
//   useScript(() => {
//     const isMobile = window.innerWidth <= 1025;
//     return isMobile ? "mobile" : "desktop";
//   }, []);

//   return typeof window !== "undefined" && window.innerWidth <= 1025
//     ? "mobile"
//     : "desktop";
// };
